<%# <div class="container bg-white justify-content-center pt-3 pb-3">
  <div class="row"> %>
    <%# <div class="col"> %>
      <%# <% if user_signed_in? %> 
      <%= render 'shared/errors', resource: @game %>
        <%= simple_form_for @game, url: private_games_path do |f| %>
          <%= f.input :title, label: 'title', placeholder: "Name of your game"%>
          <%= f.input :stake, label: 'Stake(£)' %>
          <%= f.input :start_date, label: 'Start Date'%>
            <%= f.label :deadline, 'Deadline' %>
            <%= f.datetime_local_field :deadline %>
          <%= f.input :num_players, label: 'Number of participants', placeholder: "15 is the minimum number of players required" %>
          <%#= f.association :guests, collection: User.all, input_html: { multiple: true }, label_method: lambda { |user| "#{user.first_name} #{user.last_name}" }, label_value: :id %>
          <%# <div class="home-CTA-buttons d-grid gap-2"> %>
            <%= f.submit class:"btn btn-secondary" %>
          </div>
        <% end %>
      <%# <% else %>
        <%# <p>Sorry, you must be signed in to access the event page.</p> %>
      <%# <% end %>
    <%# </div>
  </div>
</div> %>
